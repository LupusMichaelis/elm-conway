# Conway Game of life

This is an implementation of a visual sandbox to experiment with Conway's game of life.
This is been my playground to learn Elm 0.18, then 0.19.

## Usage

`bin/chisel` is the tool to manage development and deployment tasks.

Requires `docker compose` to manage development environments.

```
bin/chisel usage
```
